@inject NavigationManager Navigation
@rendermode InteractiveServer
@namespace ModalsProject

<!-- Modal -->
@if (IsVisible)
{
    <div class="modal fade show" tabindex="-1" style="display: block;" aria-labelledby="addMembersModal" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Add Members</h5>
                    <button type="button" class="btn-close" @onclick="CloseModal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <h6>Select Members to Add</h6>
                    <div class="mb-3">
                        @foreach (var member in membersList)
                        {
                            <div class="form-check">
                                <InputCheckbox id="@member.Id" class="form-check-input" @bind-Value="member.IsSelected" />
                                <label class="form-check-label" for="@member.Id">
                                    @member.Name
                                </label>
                            </div>
                        }
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @onclick="CloseModal">Close</button>
                    <button type="button" class="btn btn-primary" @onclick="AddSelectedMembers">Add Members</button>
                </div>
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public EventCallback<bool> OnClose { get; set; }
    [Parameter] public bool IsVisible { get; set; }

    private List<Member> membersList = new List<Member>
    {
        new Member { Id = 1, Name = "John Doe" },
        new Member { Id = 2, Name = "Jane Smith" },
        new Member { Id = 3, Name = "Michael Brown" },
        new Member { Id = 4, Name = "Lisa White" }
    };

    private void CloseModal()
    {
        OnClose.InvokeAsync(false);  // Close the modal
    }

    private void AddSelectedMembers()
    {
        // Logic to handle adding selected members
        var selectedMembers = membersList.Where(m => m.IsSelected).ToList();
        Console.WriteLine($"Selected Members: {string.Join(", ", selectedMembers.Select(m => m.Name))}");

        // Close the modal after adding members
        CloseModal();
    }

    public class Member
    {
        public int Id { get; set; }
        public string Name { get; set; }
        public bool IsSelected { get; set; } // Track whether the member is selected
    }
}

